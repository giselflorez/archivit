# Core web framework
flask>=3.0.0
gunicorn>=21.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0

# Search functionality (required for core features)
txtai>=9.0.0
sentence-transformers>=2.7.0

# Use CPU-only torch for cloud deployment (much smaller)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# Blockchain tracking
web3>=6.0.0

# Background jobs
schedule>=1.2.0
aiohttp>=3.9.0

# Numpy
numpy>=2.0.0
