# Web deployment requirements (lighter for cloud hosting)
# Use this for Railway/Render free tier

flask>=3.0.0
gunicorn>=21.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0

# Blockchain tracking
web3>=6.0.0

# Background jobs
schedule>=1.2.0
aiohttp>=3.9.0

# Note: Heavy ML dependencies (torch, transformers, whisper) excluded
# Visual Translator and AI features will be disabled in cloud deployment
# For full features, use local deployment or paid hosting tier
